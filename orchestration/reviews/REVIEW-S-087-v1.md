# REVIEW-S-087-v1: RegionManager 씬 전환 중 입력 차단

**Task:** S-087
**리뷰 일시:** 2026-04-04
**대상 커밋:** N/A (구현되지 않음)

---

## 검증 결과

> 명세된 기능이 전혀 구현되지 않아 대부분의 검증이 불가능함.

### 검증 1: 엔진 검증
| 확인 항목 | 결과 | 비고 |
|----------|------|------|
| GameManager 씬 로드 체계 | ✅ | 기존 OnSceneLoadedCleanup 존재 |
| PlayerController 컴포넌트 | ✅ | 정상 존재 |
| UIManager InputBlocked 체계 | ✅ | 기존 구현 확인 |
| Rigidbody2D 참조 | ✅ | PlayerController에 정상 존재 |

### 검증 2: 코드 추적
| 확인 항목 | 결과 | 비고 |
|----------|------|------|
| IsTransitioning 플래그 | ❌ | GameManager.cs에 존재하지 않음 |
| PlayerController 입력 차단 로직 | ❌ | 씬 전환 체크 없음 |
| SceneManager 로드 연동 | ❌ | 전환 시작/완료 플래그 설정 없음 |
| AutoSave 전환 차단 | ❌ | 관련 로직 없음 |
| Rigidbody2D 속도 제로화 | ❌ | 전환 시작 시 속도 제거 없음 |

### 검증 3: UI 추적
| 확인 항목 | 결과 | 비고 |
|----------|------|------|
| 입력→이벤트→반응 체인 | N/A | 구현되지 않음 |
| UIManager IsInputBlocked 확장 | ❌ | 전환 상태 체크 미추가 |

### 검증 4: 플레이 시나리오
| 시나리오 | 결과 | 비고 |
|---------|------|------|
| 포털 이용 중 이동 입력 | ❌ | 차단되지 않음 (구현 없음) |
| 씬 로딩 중 스킬 사용 | ❌ | 차단되지 않음 (구현 없음) |
| 전환 완료 후 입력 복구 | ❌ | 테스트 불가 (구현 없음) |

---

## 페르소나 리뷰

## 🎮 미나 (캐주얼 게이머)
### 인상
아예 구현이 안 되어 있다니... 이상하네. 포털 써도 캐릭터가 멈추지 않는다는 건가? 

### 문제점
- 로딩 화면에서 캐릭터가 움직이면 완전 어색할 것 같은데
- 포털 들어갔는데 계속 움직이면 이상한 곳으로 튀어나올 수도 있을 것 같아

### 제안
- 일단 구현부터 해주세요
- 로딩 중엔 캐릭터 완전히 얼려두는 게 자연스러울 것 같아요

## ⚔️ 타쿠야 (코어 게이머)
### 인상
SPEC은 명확한데 구현이 전무하다. In Review 상태로 올린 것 자체가 품질 관리 실패.

### 문제점
- 씬 전환 중 입력 버퍼링으로 인한 순간이동 버그 방지가 핵심인데 아예 안 함
- AutoSave도 전환 중 차단해야 하는데 그것도 없음
- 기존 UIManager.IsInputBlocked() 체계와 통합할 좋은 기회를 놓침

### 제안
- R1, R2, R3 요구사항 모두 정확히 구현할 것
- 기존 inputBlocked 가드와 일관성 있게 설계할 것

## 🎨 하루카 (UX/UI 디자이너)
### 인상
사용자 경험 관점에서 필수적인 기능인데 구현되지 않음. 전환 중 입력이 쌓이면 UI가 예측 불가능해짐.

### 문제점
- 시각적 피드백 없이 입력만 막으면 반응성이 떨어져 보일 수 있음
- Rigidbody2D 속도 제로화는 필수 - 캐릭터가 미끄러지면 부자연스러움

### 제안
- 로딩 인디케이터나 페이드와 연동하여 시각적 피드백 제공
- 전환 시작 시 즉시 모든 입력과 움직임 차단

## 🔍 켄지 (QA 엔지니어)
### 인상
치명적 수준의 미구현 상태. 테스트조차 불가능한 상황.

### 문제점
- SPEC 수용 기준 4개 중 어느 것도 충족되지 않음
- 경합 조건 방지 로직 전무 (씬 로드 중 여러 입력 처리)
- 예외 처리 없음 (씬 로드 실패 시 플래그 복구)

### 제안
- 구현 완료 후 전체 시나리오 재테스트 필요
- 씬 로드 실패/취소 케이스의 플래그 복구 로직 필수
- 기존 Frozen/IsInputBlocked 체계와의 상호작용 검증 필요

---

## 종합 판정

| 페르소나 | 판정 | 핵심 사유 |
|---------|------|----------|
| 미나 | ❌ | 구현되지 않음, 사용자 혼란 예상 |
| 타쿠야 | ❌ | SPEC 요구사항 전면 미구현 |
| 하루카 | ❌ | UX 필수 기능 부재 |
| 켄지 | ❌ | 테스트 불가능한 상태 |

**최종 권고:** NEEDS_WORK

---

## 크리티컬 이슈

1. **전면 미구현**: 명세된 IsTransitioning 플래그, PlayerController 입력 차단, AutoSave 차단 등 모든 요구사항이 구현되지 않음
2. **잘못된 상태 관리**: 구현되지 않은 태스크가 In Review 상태로 표시됨
3. **품질 게이트 실패**: 코드 리뷰 전에 기본 구현 완료를 확인하지 않음

이 태스크는 In Progress 상태로 되돌리고 실제 구현 완료 후 다시 리뷰를 요청해야 함.